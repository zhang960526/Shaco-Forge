# P2 — Host-only Worker

Status: NOT_DETAILED

Implement the production long-running Worker as the proven Harness Host shape. Desktop must not be required for runtime. Use the packaging/runtime strategy proven in P0.S.

Key outcomes: bootstrap, single instance, health, persistence, graceful stop, crash/restart, packaged launch.

Gate placeholder: `WORKER_HOST_READY = YES`.

## Detailed Contract

TBD after its dedicated design/review stage.
